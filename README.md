# smp-tee-runtime
minimal runtime environment written in Rust that executes critical network aggregation logic (e.g., Multi-Krum, Federated Averaging) entirely within hardware-enforced Trusted Execution Environments (TEEs) such as AMD SEV-SNP, Intel SGX/TDX, or AWS Nitro Enclaves.
